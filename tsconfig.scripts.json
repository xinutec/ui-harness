{
  "// why": "scripts/ is real code and must be type-checked like real code. tsconfig.build.json cannot do it — its rootDir is src/, so anything outside would be emitted into the published dist/. This config type-checks only (noEmit), and scripts/verify.sh runs it.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "skipLibCheck": true,
    "verbatimModuleSyntax": true,
    "allowImportingTsExtensions": true,
    "types": ["node"],
    "noEmit": true
  },
  "include": ["scripts/**/*.ts"]
}
